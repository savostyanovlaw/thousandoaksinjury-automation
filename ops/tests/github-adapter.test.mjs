import test from 'node:test';import assert from 'node:assert/strict';import {normalizeGithubItems} from '../functions/_lib/github.js';test('normalizes issues and PRs read-only',()=>{const x=normalizeGithubItems([{number:7,title:'SEO',state:'open',html_url:'https://github/x',pull_request:{},updated_at:'now'}],[{number:7,draft:false,mergeable:true}]);assert.equal(x[0].kind,'PR');assert.equal(x[0].mergeable,true)})
