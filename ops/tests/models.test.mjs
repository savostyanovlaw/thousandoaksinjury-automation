import test from 'node:test';import assert from 'node:assert/strict';import {normalizeAgentState,sanitizeOperationalText,isStale,assertNoLeadPII} from '../functions/_lib/models.js';test('stale status is derived from heartbeat age',()=>{const now=new Date('2026-09-11T20:00:00Z');assert.equal(normalizeAgentState({agent_id:'a',display_name:'A',status:'running',last_heartbeat_at:'2026-09-11T19:00:00Z'},now).status,'stale');assert.equal(isStale('2026-09-11T19:55:00Z',now,15),false)});test('secret-like operational text is redacted',()=>assert.equal(sanitizeOperationalText('Bearer abcdefghijklmnop'),'[REDACTED]'));test('lead PII fields are rejected',()=>assert.throws(()=>assertNoLeadPII({landing_page:'/',message:'secret'}),/forbidden field/))
