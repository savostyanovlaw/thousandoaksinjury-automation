import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';const sql=await readFile(new URL('../schema/0001_initial.sql',import.meta.url),'utf8');test('schema has all control center tables and no narrative column',()=>{for(const t of ['agents','tasks','activity_events','opportunities','lead_attribution','reports','external_refs','sessions'])assert.match(sql,new RegExp(`CREATE TABLE IF NOT EXISTS ${t}`));assert.doesNotMatch(sql,/case_description|narrative|full_message/i)})
