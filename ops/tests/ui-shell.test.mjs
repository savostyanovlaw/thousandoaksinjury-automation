import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';const html=await readFile(new URL('../public/index.html',import.meta.url),'utf8');const headers=await readFile(new URL('../_headers',import.meta.url),'utf8');test('mission control shell has required private sections',()=>{for(const id of ['agent-grid','activity-feed','task-board','opportunities','github-work','lead-board','reports','user-menu'])assert.match(html,new RegExp(`id=["']${id}["']`));assert.match(html,/name="robots" content="noindex,nofollow,noarchive"/i);assert.doesNotMatch(html,/(ghp_|github_pat_|RESEND_API_KEY|OPS_INGEST_TOKEN\s*=)/);assert.match(headers,/X-Robots-Tag: noindex, nofollow, noarchive/)})
