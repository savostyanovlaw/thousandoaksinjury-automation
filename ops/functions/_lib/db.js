export async function all(db,sql,...bind){const q=db.prepare(sql);return (await (bind.length?q.bind(...bind):q).all()).results||[]}export async function first(db,sql,...bind){const q=db.prepare(sql);return await (bind.length?q.bind(...bind):q).first()}export async function run(db,sql,...bind){const q=db.prepare(sql);return await (bind.length?q.bind(...bind):q).run()}export function json(body,status=200,headers={}){return new Response(JSON.stringify(body),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-robots-tag':'noindex, nofollow, noarchive',...headers}})}
