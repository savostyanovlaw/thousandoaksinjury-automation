import {requireSession} from '../_lib/auth.js';import {json} from '../_lib/db.js';import {githubGet,normalizeGithubItems} from '../_lib/github.js';export async function onRequestGet(context){const a=await requireSession(context);if(a.response)return a.response;try{const[issues,prs]=await Promise.all([githubGet('/repos/savostyanovlaw/thousandoaksinjury-automation/issues?state=open&per_page=50',context.env.GITHUB_OPS_TOKEN),githubGet('/repos/savostyanovlaw/thousandoaksinjury-automation/pulls?state=open&per_page=50',context.env.GITHUB_OPS_TOKEN)]);return json({items:normalizeGithubItems(issues,prs),source_health:'ok'})}catch{return json({items:[],source_health:'failed',message:'GitHub data temporarily unavailable'},200)}}
