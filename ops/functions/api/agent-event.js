import {json} from '../_lib/db.js';import {ingestEvent} from '../_lib/ingest.js';export async function onRequestPost({request,env}){if(!env.OPS_INGEST_TOKEN||request.headers.get('authorization')!==`Bearer ${env.OPS_INGEST_TOKEN}`)return json({ok:false,error:'Unauthorized'},401);let body;try{body=await request.json()}catch{return json({ok:false,error:'Invalid JSON'},400)}try{const e=await ingestEvent(env.OPS_DB,body);return json({ok:true,event_id:e.event_id})}catch{return json({ok:false,error:'Invalid event'},400)}}
