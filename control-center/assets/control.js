const $=s=>document.querySelector(s); const escapeHtml=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
let state=null;
function pill(status){const cls=String(status).replaceAll(' ','-');return `<span class="status-pill ${cls}">${escapeHtml(status)}</span>`}
function fmt(t){if(!t)return '—'; try{return new Intl.DateTimeFormat(undefined,{dateStyle:'medium',timeStyle:'short'}).format(new Date(t))}catch{return t}}
async function api(path,options={}){const r=await fetch(path,{...options,headers:{'content-type':'application/json',...(options.headers||{})}});const d=await r.json().catch(()=>({error:'Unexpected response'}));if(!r.ok)throw new Error(d.error||`Request failed (${r.status})`);return d}
function overall(s){if(s.summary.needsAttention)return 'NEEDS ATTENTION';if(s.summary.waitingApproval)return 'WAITING APPROVAL';if(s.summary.running)return 'RUNNING';return 'HEALTHY'}
function diagnosticLabel(s){const d=s.githubDiagnostics;if(!d)return null;const message=[d.authMessage,d.repoMessage,d.workflowMessage].find(Boolean);return `GitHub auth ${Number(d.authStatus||0)} · repo ${Number(d.repoStatus||0)} · workflow ${Number(d.workflowStatus||0)}${message?` · ${message}`:''}`}
function render(s){state=s; const o=overall(s); const p=$('#overall-status');p.textContent=o;p.className=`status-pill ${o.replaceAll(' ','-')}`;$('#last-refresh').textContent=fmt(s.generatedAt);$('#metric-production').textContent=s.summary.productionWebsite||'UNKNOWN';$('#metric-healthy').textContent=s.summary.healthy;$('#metric-running').textContent=s.summary.running;$('#metric-attention').textContent=s.summary.needsAttention;$('#metric-approval').textContent=s.summary.waitingApproval;const diag=diagnosticLabel(s);$('#connection-label').textContent=diag||(s.stale?'Some data is temporarily stale':(s.writeActionsAvailable?'Private control plane healthy':'Monitoring healthy · GitHub write actions not configured'));
  $('#agent-grid').innerHTML=s.agents.map(a=>`<article class="agent-card" data-agent="${escapeHtml(a.id)}"><div class="agent-head"><div><h3>${escapeHtml(a.name)}</h3><p>${escapeHtml(a.description||'')}</p></div>${pill(a.status)}</div><div class="agent-meta"><div>Last run<strong>${fmt(a.lastRun?.createdAt)}</strong></div><div>Next expected<strong>${fmt(a.nextExpectedAt)}</strong></div><div>Findings<strong>${a.issues?.length||0}</strong></div><div>Approvals<strong>${a.pendingApprovals||0}</strong></div></div><div class="agent-actions">${(a.capabilities||[]).filter(c=>c.autonomy!=='RED').map(c=>`<button class="action-btn" data-command="${escapeHtml(c.name)}" data-agent-id="${escapeHtml(a.id)}" ${s.writeActionsAvailable?'':'disabled title="GitHub write actions are not configured"'}>${escapeHtml(c.name.replaceAll('_',' '))}</button>`).join('')}<button class="ghost" data-detail="${escapeHtml(a.id)}">Details</button></div></article>`).join('');
  $('#approval-count').textContent=s.approvals.length; $('#approval-list').innerHTML=s.approvals.length?s.approvals.map(a=>`<article class="approval-item"><div><strong>${escapeHtml(a.action)} · ${escapeHtml(a.agentId)}</strong><p>${escapeHtml(a.targetType)} ${escapeHtml(a.targetId)} · revision ${escapeHtml(a.targetRevision)}</p></div><div class="approval-actions"><button class="approve-btn" data-review-approval="${escapeHtml(a.id)}">Review</button></div></article>`).join(''):'<div class="empty-state">No actions are waiting for approval.</div>';
  $('#attention-list').innerHTML=s.attention.length?s.attention.map(x=>`<div class="feed-item"><strong>${escapeHtml(x.title)}</strong><small>${x.issues?.length||0} unresolved finding(s)</small></div>`).join(''):'<div class="empty-state">Nothing needs your attention.</div>';
  $('#activity-list').innerHTML=s.activity.length?s.activity.map(x=>`<div class="feed-item"><strong>${escapeHtml(x.title)}</strong><small>${fmt(x.timestamp)} · ${escapeHtml(x.status)}</small></div>`).join(''):'<div class="empty-state">No recent activity.</div>';
}
async function load(){try{render(await api('/api/control/state'))}catch(e){$('#connection-label').textContent='Control API unavailable';$('#agent-grid').innerHTML=`<div class="empty-state">${escapeHtml(e.message)}</div>`}}
async function runCommand(agentId,command,button){if(!state?.writeActionsAvailable)return;button.disabled=true;const old=button.textContent;button.textContent='Running…';try{await api('/api/control/commands',{method:'POST',body:JSON.stringify({agentId,command,idempotencyKey:crypto.randomUUID()})});button.textContent='Dispatched';setTimeout(load,1800)}catch(e){button.textContent='Failed';alert(e.message)}finally{setTimeout(()=>{button.disabled=false;button.textContent=old},2200)}}
async function showAgent(id){try{const d=await api(`/api/control/agents/${encodeURIComponent(id)}`);$('#dialog-title').textContent=d.agent.name;const runs=(d.agent.recentRuns||[]).slice(0,6).map(r=>`<div class="feed-item"><strong>${escapeHtml(r.conclusion||r.status||'run')}</strong><small>${fmt(r.createdAt)}</small></div>`).join('')||'<div class="empty-state">No recent runs.</div>';const issues=(d.agent.issues||[]).map(x=>`<div class="feed-item"><strong>#${Number(x.number)} ${escapeHtml(x.title)}</strong><small>Open issue</small></div>`).join('')||'<div class="empty-state">No open issues.</div>';const pulls=(d.agent.pulls||[]).map(x=>`<div class="feed-item"><strong>PR #${Number(x.number)} ${escapeHtml(x.title)}</strong><small>Open pull request</small></div>`).join('')||'<div class="empty-state">No open pull requests.</div>';$('#dialog-body').innerHTML=`<div class="detail-grid"><div class="detail-box"><span>Status</span><strong>${escapeHtml(d.agent.status)}</strong></div><div class="detail-box"><span>Last run</span><strong>${fmt(d.agent.lastRun?.createdAt)}</strong></div><div class="detail-box"><span>Next expected</span><strong>${fmt(d.agent.nextExpectedAt)}</strong></div><div class="detail-box"><span>Pending approvals</span><strong>${d.agent.pendingApprovals||0}</strong></div></div><h3>Recent runs</h3><div class="feed">${runs}</div><h3>Open issues</h3><div class="feed">${issues}</div><h3>Open pull requests</h3><div class="feed">${pulls}</div>`;$('#agent-dialog').showModal()}catch(e){alert(e.message)}}
function showApproval(id){const a=state?.approvals.find(x=>x.id===id);if(!a)return;$('#approval-dialog-body').innerHTML=`<p>This approval is valid only for the exact target below. If the target revision changes, approval will be rejected as stale.</p><div class="target-code">Agent: ${escapeHtml(a.agentId)}<br>Action: ${escapeHtml(a.action)}<br>Target: ${escapeHtml(a.targetType)} ${escapeHtml(a.targetId)}<br>Revision: ${escapeHtml(a.targetRevision)}</div><div class="approval-actions dialog-actions"><button class="approve-btn" data-decision="APPROVE" data-approval-id="${escapeHtml(a.id)}">Approve exact action</button><button class="danger-btn" data-decision="REJECT" data-approval-id="${escapeHtml(a.id)}">Reject</button></div>`;$('#approval-dialog').showModal()}
async function decide(id,decision,btn){btn.disabled=true;try{await api(`/api/control/approvals/${encodeURIComponent(id)}`,{method:'POST',body:JSON.stringify({decision})});$('#approval-dialog').close();await load()}catch(e){alert(e.message)}finally{btn.disabled=false}}
document.addEventListener('click',e=>{const cmd=e.target.closest('[data-command]');if(cmd && !cmd.disabled)runCommand(cmd.dataset.agentId,cmd.dataset.command,cmd);const det=e.target.closest('[data-detail]');if(det)showAgent(det.dataset.detail);const rev=e.target.closest('[data-review-approval]');if(rev)showApproval(rev.dataset.reviewApproval);const dec=e.target.closest('[data-decision]');if(dec)decide(dec.dataset.approvalId,dec.dataset.decision,dec)});$('#refresh').addEventListener('click',load);$('#dialog-close').addEventListener('click',()=>$('#agent-dialog').close());$('#approval-close').addEventListener('click',()=>$('#approval-dialog').close());load();
