const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const p=JSON.parse(fs.readFileSync(path.join(process.cwd(),'control-center/functions/api/bootstrap/access-policy.json'),'utf8'));test('policy fixture targets only owner and control center',()=>{assert.equal(p.application.domain,'slc-ai-control.pages.dev');assert.deepEqual(p.policy.include,[{email:{email:'savostyanovlaw@gmail.com'}}]);});
