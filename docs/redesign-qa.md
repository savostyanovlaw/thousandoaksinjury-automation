# Premium Redesign QA Record

Date: 2026-09-11

## Scope

Working branch: `claude/premium-redesign-v2`
Base branch: `redesign/premium-v2`
Production/default branch: `main` (not targeted or modified by this redesign workflow)

Implemented redesign coverage:
- `/`
- `/car-accident-lawyer/`
- `/newbury-park/`
- `/agoura-hills/`
- `/camarillo/`
- `/oak-park/`
- `/simi-valley/`
- `/westlake-village/`
- `/ru/`
- `404.html`
- shared `assets/city-pages.css`
- shared `assets/city-pages.js`
- `sitemap.xml`
- Cloudflare Pages Function `/api/case-review`

## Automated verification

GitHub Actions workflow: `Premium Redesign Validation`
Form-integration verification run: `34632411387`
Head SHA verified by that run: `4a0205cc6f4e8b4f6ddb7d8568f63e61452d3f36`

That run completed all automated gates:
1. `python3 -m unittest discover -s tests -v` — 23 tests, all OK.
2. `python3 scripts/validate_site.py --root website` — `Site validation passed`.
3. `node scripts/browser_smoke.js` — `Browser smoke passed for 3 engines, 6 routes, and 5 viewport widths.`
4. A one-time POST to the live Cloudflare branch preview endpoint `/api/case-review` returned HTTP 200 with `{"ok":true}`, confirming that the deployed Pages Function could read the Preview `RESEND_API_KEY` secret and that Resend accepted the test submission.

The automated contract covers, among other checks:
- premium design-system selectors and design tokens;
- reduced-motion support;
- accessible mobile-navigation interaction contract;
- consent-gated GA4 and privacy contract;
- progressive enhancement markers;
- secure case-review function configuration and server-side Resend secret usage;
- English and Russian intake-form field contracts;
- form validation/progressive enhancement and honeypot anti-spam behavior;
- all six local landing pages using the premium template;
- visible phone and email on local pages;
- one H1 on local pages;
- banned/unapproved marketing claims;
- canonical URLs;
- JSON-LD parsing;
- sitemap route coverage;
- internal-link resolution;
- no unexpected `noindex`;
- Russian page bilingual hreflang and known untranslated-copy checks.

## Cross-browser responsive QA

The Playwright smoke test ran against a locally served copy of the redesign in:
- Chromium
- Firefox
- WebKit

Representative routes tested:
- `/`
- `/car-accident-lawyer/`
- `/newbury-park/`
- `/agoura-hills/`
- `/ru/`
- `/404.html`

Viewport widths tested:
- 360 px
- 390 px
- 768 px
- 1024 px
- 1440 px

For each engine/route/viewport combination, the smoke test checks that the page responds successfully, renders exactly one H1, exposes the firm phone and email links, has no horizontal document overflow, and reports no browser console or page errors.

The form-integration verification run generated and uploaded 36 full-page screenshots at 390 px and 1440 px across the three browser engines and six representative routes. Artifact: `premium-redesign-browser-screenshots`, artifact ID `10276766622`, generated by run `34632411387`.

The screenshots are retained as review evidence. Their presence and successful generation are verified; this record does not claim a manual aesthetic inspection of every screenshot.

## Accessibility and privacy safeguards implemented

- Skip links and semantic main/header/nav/footer structure are present on redesigned pages.
- Mobile navigation uses `aria-expanded`, Escape-key behavior, and a shared accessible interaction contract.
- Focus-visible and minimum touch-target rules are part of the shared premium stylesheet.
- Motion enhancements are disabled/reduced under `prefers-reduced-motion`.
- Core content remains present in HTML without requiring JavaScript to become crawlable.
- Existing consent storage key `toi_cookie_consent` and consent-gated GA4 behavior are preserved.
- YouTube privacy behavior remains click-to-load via the existing privacy-preserving implementation.

## SEO/content safeguards

- Existing indexed route structure is preserved.
- Canonicals, page titles, local H1s, local FAQ structured data, internal city links, robots rules, and sitemap coverage are retained or refreshed.
- Phone `(818) 213-8798` and `attorney@savostyanovlaw.com` are visible rather than only machine-readable.
- The redesign does not publish unverified ratings, recovered-dollar claims, success rates, awards, or guaranteed-outcome language.
- Local pages contain distinct community-specific content rather than identical thin clones.
- `/ru/` is a genuine Russian-language page rather than an automatic translation widget.

## Contact form

The redesign now has a server-side form delivery path:
- Browser forms are progressively enabled by shared JavaScript and POST to `/api/case-review`.
- Cloudflare Pages Function `functions/api/case-review.js` validates and limits submitted fields, uses a honeypot for basic bot filtering, and sends through Resend.
- Resend credentials are read only from the server-side Cloudflare secret `RESEND_API_KEY`; the API key is not embedded in HTML or JavaScript.
- Submission emails are addressed to `attorney@savostyanovlaw.com` and originate from `intake@forms.thousandoaksinjury.com`, with the prospective client's email set as Reply-To.
- The one-time live preview integration test returned `{"ok":true}` from the deployed endpoint. This confirms provider acceptance; it does not by itself prove mailbox delivery.

The remaining production integration gate is to add the same `RESEND_API_KEY` as an encrypted secret in the Cloudflare **Production** environment before production deployment, then perform a production-safe post-deploy submission check. The production secret has intentionally not been configured yet.

## Performance evidence limitation

No Lighthouse or field Core Web Vitals run is claimed in this QA record. Those metrics should only be recorded from an actual supported run or production/preview measurement; no score is inferred or fabricated here.

## Production safety

No merge to `main` or production website deployment has been performed by this redesign workflow. Production form credentials also remain unconfigured. Those remain separate approval-gated actions.
