GREEN verification is enforced by `.github/workflows/russian-language-robot-validation.yml` on the pull request. It executes `python -m unittest tests.test_russian_language_robot` on the branch head.
