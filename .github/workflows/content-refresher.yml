name: Content Refresher

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'scripts/content_refresher.py'
      - 'tests/test_content_refresher.py'
      - '.github/workflows/content-refresher.yml'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m unittest tests.test_content_refresher -v
